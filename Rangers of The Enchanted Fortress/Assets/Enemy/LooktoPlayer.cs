using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class LooktoPlayer : MonoBehaviour
{
    public Transform player;
    public float rotationSpeed=0.08f;
    // Update is called once per frame
    void Update()
    {
        Debug.Log("ray");
       // Ray interactionRay =
        RaycastHit interactionInfo;

        //check the terrain position or other object below the mouse
       // if (Physics.SphereCast(transform.position,30f, vector3 , out interactionInfo, Mathf.Infinity))//&&ShadowDetection.isInShadow)
      /*  {
            Debug.Log("test");
            //Get the game object Terrain or object below the mouse
            GameObject interactedObject = interactionInfo.collider.gameObject;
            Vector3 direction = (transform.position - interactedObject.transform.position);
            Quaternion lookRotation = Quaternion.LookRotation(direction);
            transform.rotation = Quaternion.Slerp(transform.rotation, lookRotation, rotationSpeed);
          
        }
        
        //The last parameter here controls the speed of the rotation
        

    }
        
      

}*/

